# Routing eval — 2026-09-26

Run with `python3 scripts/eval_routing.py --runs 2` on the 30 cases in `tests/evals/routing.json` (10 per plugin, Spanish and English), loading god-mode-core, god-mode-dev and god-mode-growth together. Model: claude-sonnet-5. Both versions ran at the same time so they share conditions.

| Version | Correct skill | Other skill | No skill used | Failed runs |
| --- | --- | --- | --- | --- |
| Before sprint 5 (long descriptions) | 19 / 60 | 0 | 41 | 0 |
| After sprint 5 (descriptions of 300 characters or fewer) | 21 / 60 | 0 | 39 | 0 |

- When Claude used a skill, it was the right one every time, before and after.
- Every miss is a run where Claude answered directly instead of loading a skill, usually to ask for context the request did not include (a URL, a repository, a file). The difference between the two versions is within run-to-run noise.
- Skill names and descriptions shrank from about 16,400 tokens (135 skills) to about 7,300 tokens (106 skills), with no loss in routing accuracy.

Next step for this eval: give each request the context it needs (a sample repo, a URL) so the measure reflects skill selection rather than Claude's choice to ask first.
